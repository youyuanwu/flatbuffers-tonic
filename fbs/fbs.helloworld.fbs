// FB cpp does not require namespace, but tonic requires it.
namespace fbs.helloworld;

attribute "streaming";

table HelloRequest {
    name:string;
}

table HelloReply {
    message:string;
}

root_type HelloReply;

rpc_service Greeter {
    SayHello(HelloRequest): HelloReply (streaming: "none");
}